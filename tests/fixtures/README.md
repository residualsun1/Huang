# 测试夹具

这里存放只用于自动测试、不会发布到网站的固定示例。

为什么需要夹具：

- `content/` 中的正式文章属于可变内容，可以新增、改名或删除。
- notice、表格、脚注等渲染能力属于稳定功能，需要长期保留测试。
- 如果直接拿正式文章做功能样本，删除文章就会让测试误报失败。

维护原则：

1. 测试一种 Markdown 功能时，优先修改 `markdown/` 下的示例。
2. 不要把夹具放进 `content/`，否则它会被构建成公开文章。
3. 真实网站测试只检查动态关系，例如链接目标是否存在，不固定文章标题或 slug。
